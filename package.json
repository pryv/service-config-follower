{
  "name": "service-config-follower",
  "version": "1.0.0",
  "description": "A micro-service who will fetch configurations from service-config-leader (on register), put them as file in the corresponding service folder and restart the service.",
  "main": "index.js",
  "author": "Loïc Correvon",
  "license": "MIT",
  "scripts": {
    "start": "NODE_TLS_REJECT_UNAUTHORIZED='0' node src/server.js --config dev-config.json",
    "test": "NODE_ENV=test node_modules/.bin/mocha --recursive test/"
  },
  "dependencies": {
    "express": "^4.17.1",
    "js-yaml": "^3.14.0",
    "nconf": "^0.10.0",
    "sinon": "^9.0.3",
    "superagent": "^3.8",
    "url": "^0.11.0",
    "winston": "^2.3.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "fs-extra": "^8.1.0",
    "mocha": "^6.2.0",
    "nock": "^10.0.6",
    "supertest": "^4.0.2"
  }
}
